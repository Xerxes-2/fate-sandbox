# Turn Reminder

- Resolve tools before claiming any mechanical change; tool returns override the brief.
- One player action window per turn; stop after major settlement.
- Secrets never enter player-safe fields; encode them as traces or consequences.
- Finish the turn with exactly one `submit_direction_packet` call. No narrative text outside tools.
